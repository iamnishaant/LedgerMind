# Backend Python dependencies
# AI FinanceOS — Phase 1 MVP

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9      # for file uploads

# LangGraph & LangChain
langgraph==0.2.28
langgraph-checkpoint-postgres<2.0.0   # durable HITL checkpointer (uses DATABASE_URL); 1.x matches langgraph 0.2.x
langchain==0.3.0
langchain-openai==0.2.0
langchain-anthropic==0.2.0
langchain-community==0.3.0
psycopg[binary,pool]==3.2.3             # async Postgres driver for the checkpointer

# OCR
# NOTE: paddleocr 2.7.3 pins opencv-python<=4.6.0.66, which has no Python 3.11
# wheel. Install paddleocr AFTER this file, without deps (works with OpenCV 4.10
# at runtime):   pip install paddleocr==2.7.3 --no-deps
paddlepaddle==2.6.2
numpy==1.26.4                    # paddle 2.6.x requires numpy < 2
opencv-contrib-python==4.10.0.84 # OpenCV 4.x (paddleocr is not compatible with 5.x)
opencv-python==4.10.0.84
# paddleocr==2.7.3  →  installed separately with --no-deps (see note above)
# paddleocr runtime deps (so --no-deps doesn't leave gaps):
shapely
pyclipper
lmdb
imgaug
rapidfuzz
python-docx

# Supabase (2.15+ required for the new sb_publishable_/sb_secret_ API key format)
supabase==2.31.0

# Inngest (event-driven)
inngest==0.4.14

# Token encryption for connected accounts (Phase 8) — was already a transitive
# dep of python-jose[cryptography]; pinned explicitly because crypto.py imports it
cryptography>=42.0

# Data & Validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Image processing
Pillow==10.4.0
pdf2image==1.17.0

# Utilities
httpx==0.27.2
tenacity<9.0.0             # retry logic for LLM calls (langchain 0.3 requires <9)
python-jose[cryptography]==3.3.0
