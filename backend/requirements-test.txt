# Test-only install — deliberately NOT `-r requirements.txt`.
#
# Since ocr_agent.py's `from paddleocr import PaddleOCR` was made lazy
# (deferred into get_ocr_engine(), see that function's docstring), none of the
# tests in backend/tests/ need paddleocr/paddlepaddle/opencv/shapely/
# pyclipper/lmdb/imgaug/rapidfuzz installed at all — real OCR-against-images
# testing is intentionally separate (backend/scripts/verify_ocr.py), runs less
# often, and needs the full requirements.txt. This file keeps CI fast.
#
# Install order for CI/local:  pip install -r requirements-test.txt

# Web framework (app.core.auth raises fastapi.HTTPException; main.py boot test)
fastapi==0.115.0
slowapi==0.1.10
python-multipart==0.0.9

# LangGraph & LangChain (orchestrator, agents, durable checkpointer)
langgraph==0.2.28
langgraph-checkpoint-postgres<2.0.0
langchain==0.3.0
langchain-openai==0.2.0
langchain-anthropic==0.2.0
langchain-community==0.3.0
psycopg[binary,pool]==3.2.3

# Supabase
supabase==2.31.0

# Inngest (main.py boot test mounts it)
inngest==0.4.14

# Token encryption
cryptography>=42.0

# Data & validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Image handling used directly by ocr_agent.py's regex/parsing path (NOT
# PaddleOCR itself — Pillow/numpy are lightweight, always needed)
Pillow==10.4.0
numpy==1.26.4

# Utilities
httpx==0.27.2
tenacity<9.0.0
python-jose[cryptography]==3.3.0

# Test runner
pytest==9.1.1
pytest-asyncio==1.4.0
